<%= render 'shared_content/title', title: @villain.name %>

<h4>Set: <%= link_to @villain.expansion.name, expansion_path(@villain.expansion) %></h4>

<table class='table table-striped table-bordered'>
  <caption>Stats for <%= @villain.name %></caption>
  <thead>
    <tr>
      <th>Number of Players</th>
      <th>Games Played</th>
      <th>Win Rate</th>
    </tr>
  </thead>
  <tbody>
    <% 1.upto(5) do |x| %>
      <tr>
        <th scope="row"><%= x %></th>
        <td><%= @villain.total_games_with_player(x) %></td>
        <td><%= @villain.show_villain_win_rate_with_player(x) %>%</td>
      </tr>
    <% end %>
    <tr>
      <th scope="row">Overall</th>
      <td><%= @villain.total_games %></td>
      <td><%= @villain.show_villain_win_rate %>%</td>
    </tr>
  </tbody>
</table>