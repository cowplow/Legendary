<%= render 'shared_content/title', title: @henchman.name %>

<h4>Set: <%= link_to @henchman.expansion.name, expansion_path(@henchman.expansion) %></h4>

<table class='table table-striped table-bordered'>
  <caption>Stats for <%= @henchman.name %></caption>
  <thead>
    <tr>
      <th>Number of Players</th>
      <th>Games Played</th>
      <th>Win Rate</th>
    </tr>
  </thead>
  <tbody>
    <% 1.upto(5) do |x| %>
      <tr>
        <th scope="row"><%= x %></th>
        <td><%= @henchman.total_games_with_player(x) %></td>
        <td><%= @henchman.show_henchmen_win_rate_with_player(x) %>%</td>
      </tr>
    <% end %>
    <tr>
      <th scope="row">Overall</th>
      <td><%= @henchman.total_games %></td>
      <td><%= @henchman.show_henchmen_win_rate %>%</td>
    </tr>
  </tbody>
</table>